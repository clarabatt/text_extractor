# Text Extractor

Extracts text from pdfs and images.

## How to run

Install the dependencies:

```
poetry init
```

Running the project:

```
poetry shell
python3 manage.py runserver 0.0.0.0:8080
```
